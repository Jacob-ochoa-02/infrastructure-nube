version: 0.2
 
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - echo "=== DIRECTORIO DE TRABAJO ==="
      - pwd
      - echo "=== CONTENIDO RAIZ ==="
      - ls -la
      - echo "=== ARBOL COMPLETO (2 niveles) ==="
      - find . -maxdepth 2 -not -path '*/.git/*' -not -name '.git'
      - echo "=== FIN DIAGNOSTICO ==="